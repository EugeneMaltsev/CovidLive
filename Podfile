# Uncomment the next line to define a global platform for your project
#  platform :ios, '14.0'

target 'CovidLive (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Kanna', '~> 5.2.2'
  # Pods for CovidLive (iOS)

end

target 'CovidLive (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
   pod 'Kanna', '~> 5.2.2'
  # Pods for CovidLive (macOS)

end
